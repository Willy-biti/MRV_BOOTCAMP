let nome = 'wilyy;biti;nags';

let  resultado = nome.split(';'); //Transforma STRING em ARRAY
let  resultado = nome[4];   //Tem a mesma função  que o charAt, quando swe trata de uma string
let  resultado = nome.charAt(7); //Saber o que tá numa detereminada position
let  resultado = nome.trim(); //TRIM = TIRAR ESPAÇOS INUTEIS
let  resultado = nome.concat('CHAMPION');  //CONCATENAR
let  resultado = nome.toLocaleLowerCase();
let  resultado = nome.toUpperCase(); //toUpperCase = MAIUSCULO
let  resultado = nome.replace('biti', 'Emilie'); // REPLACE = Trocar palavra 
let  resultado = nome.substr(9, 15);
let  resultado = nome.substring(10);  //
let  resultado = nome.slice(-4);  //SLICE = Posição inicial 
let  resultado = nome.indexOf(' '); //INDEXOF = Verificara se determinada palavra tem nesta variavel
let  resultado = nome.length;  // LENGTH = CPMPRIMENTO


console.log(resultado); 

console.log( resultado);